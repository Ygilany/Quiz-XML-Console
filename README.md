Quiz XML to plain Text
==========================

### prerequistes
- Node.js

### Setup 
- from the directory install the dependencies `npm install`

### Usage
- run `node index.js XML_FILE_NAME`
i.e. `node index.js test1.xml`